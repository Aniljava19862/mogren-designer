import {useState} from 'react';import {useAuth} from '@/context/AuthContext';import {useNavigate} from 'react-router-dom';
export default function Register(){const [f,setF]=useState({name:'',email:'',password:''}),[err,setErr]=useState('');const {register}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();try{await register(f);nav('/')}catch(x){setErr(x.message)}};return <main className="max-w-md mx-auto px-4 py-16"><h1 className="text-3xl font-black">Create account</h1><form onSubmit={submit} className="space-y-4 mt-8">{err&&<p className="text-red-600">{err}</p>}{['name','email','password'].map(k=><input key={k} className="border rounded-xl px-4 py-3 w-full" type={k==='password'?'password':k==='email'?'email':'text'} placeholder={k[0].toUpperCase()+k.slice(1)} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/>)}<button className="bg-black text-white rounded-xl py-3 w-full font-bold">Register</button></form></main>}
